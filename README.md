# Movie-Recommendation-Project-MRP-
AI-powered movie recommendation system that learns from user ratings and viewing behavior using machine learning and real-time movie metadata APIs.

## Local Data and Artifacts

- Place MovieLens files in `ml/data/` (ignored by git).
- Model artifacts are saved to `ml/artifacts/` (ignored by git).
- These directories are intentionally excluded from version control.
